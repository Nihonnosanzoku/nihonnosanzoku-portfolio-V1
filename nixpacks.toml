[phases.setup]
nixPkgs = ["...", "ffmpeg", "python3", "curl", "openssl"]

[phases.install]
dependsOn = ["setup"]
cmds = ["python3 -m pip install -U yt-dlp", "pnpm install"]

[phases.build]
dependsOn = ["install"]
cmds = ["pnpm exec prisma generate", "pnpm run build"]

[start]
cmd = "pnpm run start"